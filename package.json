{
  "name": "mindspend-backend",
  "version": "0.1.0",
  "description": "Privacy-first financial tracking API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node -e \"require('dotenv').config(); require('nodemon')({ script: 'src/server.js', ext: 'js' })\"",
    "frontend": "node scripts/serve-frontend.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
