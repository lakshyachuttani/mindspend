# Server
PORT=3000

# PostgreSQL (match docker-compose service name when running in Docker)
DB_HOST=db
DB_PORT=5432
DB_NAME=mindspend
DB_USER=mindspend
DB_PASSWORD=change_me_in_production

# For local dev without Docker, use:
# DB_HOST=localhost
